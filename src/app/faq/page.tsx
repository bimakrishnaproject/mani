import UnderProgressPage from "@/components/UnderProgressPage";

export default function FAQPage() {
  return (
    <UnderProgressPage
      pageName="Frequently Asked Questions"
      description="The full FAQ directory is being populated for the next release. Return to the Homepage presentation for today's demo."
    />
  );
}
