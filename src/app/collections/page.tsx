import UnderProgressPage from "@/components/UnderProgressPage";

export default function CollectionsPage() {
  return (
    <UnderProgressPage
      pageName="Collections Directory"
      description="The full collections catalog page is currently being crafted for the next release. Today's update focuses on the Homepage presentation."
    />
  );
}
